<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <AssemblyName>navdiag</AssemblyName>
    <RootNamespace>NavDiag</RootNamespace>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>disable</Nullable>
    <!-- Scratch diagnosis harness: replays the overland planner's own queries for one zone offline
         (2026-09-25, the Newland City wompah). Not part of the bot's run. -->
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\AOBuddy\AOBuddy.csproj" />
  </ItemGroup>
</Project>