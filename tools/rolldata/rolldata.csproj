<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <AssemblyName>rolldata</AssemblyName>
    <RootNamespace>RollData</RootNamespace>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>disable</Nullable>
    <!-- Per-profession nano crystal tables for AODB's Rollable pages (2026-09-25): every crystal with its
         QL, nano line, NCU, level and nano-skill requirements and icon, from the bot's own item data. -->
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\AOBuddy\AOBuddy.csproj" />
  </ItemGroup>
</Project>
